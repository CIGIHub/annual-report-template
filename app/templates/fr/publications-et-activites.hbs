<Slides::OutputsAndActivities
  @objects={{this.objects}}
  @publication={{this.publication}}
  @totalObjects={{this.totalObjects}}
  @page={{this.page}}
  @id={{this.id}}
  @type={{this.type}}
  @setPage={{this.setPage}}
  @setType={{this.setType}}
  @setId={{this.setId}}
/>
